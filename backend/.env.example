# ─── Server ───────────────────────────────────────────────────────────────────
PORT=3001
NODE_ENV=production

# ─── Database ─────────────────────────────────────────────────────────────────
# Dev:        ./data/moviegame.db  (relative to backend/)
# Production: /data/moviegame.db  (mount a Railway/Render persistent volume at /data)
DATABASE_PATH=/data/moviegame.db

# ─── Security ─────────────────────────────────────────────────────────────────
# Generate both with: openssl rand -hex 32
COOKIE_SECRET=CHANGE_ME_long_random_string
ADMIN_PASSWORD=CHANGE_ME_strong_password
# Optional: add a username requirement for the admin login (leave empty for password-only)
ADMIN_USERNAME=

# ─── CORS ─────────────────────────────────────────────────────────────────────
# Your production domain — no trailing slash
# Railway example: https://moviegame-production.up.railway.app
CORS_ORIGIN=https://your-app.railway.app

# ─── TMDB ─────────────────────────────────────────────────────────────────────
# Get a free API key at https://www.themoviedb.org/settings/api
# Required for: random film button, backdrop picker
TMDB_API_KEY=CHANGE_ME_tmdb_api_key
TMDB_IMAGE_BASE_URL=https://image.tmdb.org/t/p/w1280

# ─── Uploads ─────────────────────────────────────────────────────────────────
# Persist on the same volume as DATABASE_PATH (recommended in Docker/Railway: /data/uploads)
UPLOADS_DIRECTORY=/data/uploads

# ─── Wiki admin prefetch pool (Au hasard) ───────────────────────────────────
# Optional — defaults : 24 / 4. Caps : 400 / 40. POST /api/admin/prefetch/warm?target= ≤ 400
# WIKI_PREFETCH_TARGET_READY=80
# WIKI_PREFETCH_MAX_FETCH_PER_RUN=8
# Limite résultats SPARQL Wikidata (sinon défaut = max(100, TARGET_READY), max 800). À augmenter si TARGET_READY > 100.
# WIKI_PREFETCH_SPARQL_LIMIT=400
# Optional cron : Authorization: Bearer ... or X-Prefetch-Warm-Token
# PREFETCH_WARM_TOKEN=

# ─── Alerte planning (e-mail, optionnel, via https://resend.com ) ─────────────
# Si RESEND_API_KEY + PLANNING_ALERT_FROM + PLANNING_ALERT_EMAIL sont définis,
# un mail est envoyé quand le max de jours « vides » (30j, par type) dépasse le seuil.
# PLANNING_ALERT_FROM = adresse d’envoi vérifiée chez Resend
# PLANNING_ALERT_EMAIL = destinataire(s), CSV autorisé
# PLANNING_ALERT_THRESHOLD=8
# PLANNING_ALERT_COOLDOWN_HOURS=24
# PLANNING_ALERT_ENABLED=1
# RESEND_API_KEY=
# PLANNING_ALERT_FROM=Planning <planning@votredomaine.com>
# PLANNING_ALERT_EMAIL=vous@example.com

# ─── Game rules ───────────────────────────────────────────────────────────────
MAX_ATTEMPTS=5
WIKI_MAX_ATTEMPTS=5
IMAGE_SOURCE=tmdb
# Set to true to allow scheduling challenges in the past (useful for testing)
ALLOW_PAST_SCHEDULING=false

# ─── Logging ──────────────────────────────────────────────────────────────────
LOG_LEVEL=info

# ─── Rate limiting ────────────────────────────────────────────────────────────
# Preset DEV (confortable)
API_RATE_LIMIT_MAX=600
API_RATE_LIMIT_WINDOW_MS=60000
GUESS_RATE_LIMIT_MAX=30
GUESS_RATE_LIMIT_WINDOW_MS=60000
SEARCH_RATE_LIMIT_MAX=60
SEARCH_RATE_LIMIT_WINDOW_MS=60000
ADMIN_RATE_LIMIT_MAX=180
ADMIN_RATE_LIMIT_WINDOW_MS=60000
LOGIN_RATE_LIMIT_MAX=10
LOGIN_RATE_LIMIT_WINDOW_MS=60000

# Preset PROD (stricte) — valeurs recommandées
# API_RATE_LIMIT_MAX=300
# API_RATE_LIMIT_WINDOW_MS=60000
# GUESS_RATE_LIMIT_MAX=30
# GUESS_RATE_LIMIT_WINDOW_MS=60000
# SEARCH_RATE_LIMIT_MAX=60
# SEARCH_RATE_LIMIT_WINDOW_MS=60000
# ADMIN_RATE_LIMIT_MAX=120
# ADMIN_RATE_LIMIT_WINDOW_MS=60000
# LOGIN_RATE_LIMIT_MAX=5
# LOGIN_RATE_LIMIT_WINDOW_MS=60000
